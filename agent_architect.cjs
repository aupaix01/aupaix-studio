const fs = require("fs"); const { exec } = require("child_process"); console.log("?? AUPAIX STUDIO AGENT GEACTIVEERD..."); const dirs = ["./src", "./agent-logs", "./studio-core"]; dirs.forEach(dir => { if (!fs.existsSync(dir)) { fs.mkdirSync(dir); console.log("? Map aangemaakt: " + dir); } }); const commercialTs = "export const CONFIG = { price: 9.99, live: false };\nexport function logSale(amt) { console.log(\"Sale recorded: \" + amt); }"; fs.writeFileSync("./src/commercial.ts", commercialTs); console.log("? Commercial Engine hersteld in src/commercial.ts"); console.log("?? Synchroniseren met GitHub..."); exec("git add . && git commit -m \"AGENT: Absolute Fix\" && git push", (err, stdout) => { if (err) console.log("? Sync fout: " + err); else console.log("? GitHub is up-to-date!"); });
