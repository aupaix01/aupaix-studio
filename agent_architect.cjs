const { exec } = require("child_process"); console.log("?? AUPAIX LIVE-DEPLOYER..."); exec("git add . && git commit -m \"FORCE DEPLOY\" && git push", (err) => { if (err) console.log("? Sync fout: " + err); else console.log("? LIVE-SYNC VOLTOOID!"); });
